# Evaluations

Evaluations compare a **candidate** model against the current **champion**.
Suites are JSON files under `ml/evals/suites/`.

Run:

```
npm run ml:eval
```

Reports are written to `ml/evals/reports/`.

# experiments/mcp

MCP demo-only files moved from `mcp/`.

- Moved files: `mcp_run_demo.ts`, `mcp_call.ts`
- Reason: Not part of production runtime or tests.
- Future: Keep only if needed for manual demonstrations.

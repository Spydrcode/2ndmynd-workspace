# Learning Layer - Python Dependencies

# Machine Learning
scikit-learn>=1.3.0
numpy>=1.24.0
joblib>=1.3.0

# Note: better-sqlite3 is installed via npm (Node.js SQLite driver)
# The Python scripts use the database created by the TypeScript storage layer
